# TensorRT-Edge-LLM 最佳端侧设备优化方案

## 一、方案概述
本方案结合 **AngelSlim 大模型压缩工具包**的算法优化能力和 **TensorRT-Edge-LLM** 的端侧部署工程能力，形成端到端的端侧大模型（LLM/VLM）优化方案，目标是在 NVIDIA Jetson 系列等端侧设备上实现：
- ✅ 内存占用降低 50%-75%
- ✅ 推理速度提升 2-4 倍
- ✅ 精度损失控制在 2% 以内
- ✅ 全自动化的量化、导出、编译、部署流水线

---

## 二、核心优化技术栈（全量覆盖AngelSlim所有优化特性）

### 2.1 量化优化（AngelSlim全量算法适配）
| 优化技术 | 适用场景 | 性能收益 | TensorRT-Edge-LLM集成方案 | **执行设备** |
|---------|----------|----------|--------------------------|--------------|
| #### FP8系列量化 | | | | |
| **FP8静态量化（fp8_static）** | 所有模型离线推理场景，平衡精度和性能 | 内存压缩4倍，推理加速2倍，精度损失<1% | 集成到量化模块，支持per-tensor/per-channel/per-group粒度配置 | ✅ **x86环境** |
| **FP8动态量化（fp8_dynamic）** | 低延迟在线推理场景 | 内存压缩4倍，推理加速1.8倍 | 运行时动态计算激活缩放因子，适配TensorRT动态形状 | ✅ **x86环境** |
| **LeptoFP8量化（增强FP8）** | 数学推理、代码生成等精度敏感任务 | 精度接近BF16，内存压缩4倍 | 替换原有FP8量化，集成AutoLayerScale缩放策略 | ✅ **x86环境** |
| #### INT8系列量化 | | | | |
| **INT8动态量化（int8_dynamic）** | 内存受限的端侧部署场景 | 内存压缩8倍，推理加速3倍 | 适配TensorRT INT8插件，支持动态形状 | ✅ **x86环境** |
| **INT8静态量化（int8_static）** | 高性能离线推理场景 | 内存压缩8倍，推理加速3.5倍 | 预校准激活值缩放因子，最大化TensorRT层融合优化 | ✅ **x86环境** |
| **SmoothQuant平滑量化** | 与INT8/FP8量化搭配使用，激活分布不均的模型（如Qwen3-VL） | 提升低比特量化精度2-5% | 作为量化前处理步骤，自动计算平滑系数 | ✅ **x86环境** |
| #### INT4系列量化 | | | | |
| **INT4-GPTQ量化** | LLM模型高性能推理，内存受限场景 | 内存压缩16倍，推理加速4倍 | 导出为TensorRT支持的INT4权重格式，对接GroupwiseGemmPlugin | ✅ **x86环境** |
| **INT4-GPTAQ量化（GPTQ改进版）** | LLM低延迟推理场景 | 精度比GPTQ提升1-2%，性能相当 | 同GPTQ集成方案，优化激活值处理逻辑 | ✅ **x86环境** |
| **INT4-AWQ量化** | 内存极其受限的端侧场景（如Orin NX 8GB） | 内存压缩16倍，推理加速3.5倍 | 导出带缩放系数的INT4权重，适配TensorRT AWQ插件 | ✅ **x86环境** |
| **W4A8INT8混合量化** | DeepSeek V3/R1等大模型极致压缩场景 | 内存压缩8-16倍，推理加速4倍 | 权重INT4 + 激活INT8混合精度导出，适配TensorRT混合精度插件 | ✅ **x86环境** |
| #### 极低比特量化 | | | | |
| **NVFP4量化** | Blackwell架构（DRIVE Thor）平台 | 内存压缩8倍，推理加速5倍 | 导出为NVFP4格式，适配TensorRT 10.x+的FP4硬件加速 | ✅ **x86环境** |
| **Tequila三值量化（2bit）** | 极端内存受限场景（如Jetson Nano <8GB） | 内存压缩32倍，支持3B模型在4GB设备运行 | 导出为三值权重格式，开发定制TensorRT三值算子 | ✅ 量化：x86环境<br>✅ 算子开发：x86环境<br>✅ 部署：端侧设备 |
| **Sherry 1.25bit量化** | 极致压缩端侧场景 | 内存压缩64倍，支持7B模型在4GB设备运行 | 同三值量化方案，适配定制硬件算子 | ✅ 量化：x86环境<br>✅ 算子开发：x86环境<br>✅ 部署：端侧设备 |
| **HY-1.8B-2Bit预训练模型** | 端侧小模型场景 | 开箱即用，内存占用<500MB | 直接导出为TensorRT引擎，无需量化 | ✅ **x86导出，端侧运行** |
| #### 多模态专属量化优化 | | | | |
| **VLM视觉模块保护量化** | 多模态模型（Qwen3-VL、HunyuanVL等） | 保持视觉精度，内存压缩4-8倍 | 量化时自动跳过视觉编码层，仅量化LLM部分 | ✅ **x86环境** |
| **Audio音频特征量化** | 音频模型（Qwen2-Audio、CosyVoice3等） | 内存压缩4倍，推理加速2倍 | 针对音频编码特征优化量化粒度 | ✅ **x86环境** |

### 2.2 推理加速（AngelSlim + TensorRT-Edge-LLM全栈优化）
| 优化技术 | 适用场景 | 性能收益 | TensorRT-Edge-LLM集成方案 | **执行设备** |
|---------|----------|----------|--------------------------|--------------|
| #### 推测解码系列 | | | | |
| **Eagle3推测解码（全模态支持）** | 所有生成式任务（LLM/VLM/Audio） | 加速比1.4-1.9倍，最高3.5倍（音频场景） | 1. x86侧用AngelSlim训练Eagle3草稿模型<br>2. 导出草稿模型ONNX并编译为TensorRT引擎<br>3. 集成到TensorRT-Edge-LLM SpecDecode运行时<br>4. 支持Tree decoding优化 | ✅ 草稿模型训练/导出：**x86环境**<br>✅ 运行时部署：**端侧设备** |
| **SpecExit思考早退** | 推理类任务（GSM8K、MMLU、代码生成等） | 在Eagle3基础上再提升10-20%加速比 | 集成到TensorRT-Edge-LLM采样器模块，根据置信度动态调整验证步数 | ✅ 模型训练/导出：**x86环境**<br>✅ 运行时部署：**端侧设备** |
| #### 通用推理优化 | | | | |
| **CUDA Graph优化** | 低延迟推理场景 | 内核启动开销降低10-30% | 扩展支持Eagle3推测解码场景的CUDA Graph捕获，支持动态batch | ✅ **端侧设备** |
| **分层KV Cache优化** | 长对话、多轮交互场景 | 内存降低30-50% | 1. 支持FP8/INT4 KV Cache存储<br>2. 集成系统Prompt缓存机制，避免重复计算 | ✅ **端侧设备** |
| #### Diffusion模型专属优化 | | | | |
| **DeepCache/TeaCache/TaylorCache缓存优化** | 文生图/视频/3D生成场景 | 速度提升2-3倍，精度损失极小 | 集成分层缓存逻辑到Diffusion模型运行时，支持块级缓存控制 | ✅ 缓存逻辑开发：**x86环境**<br>✅ 运行时部署：**端侧设备** |
| **Diffusion FP8量化** | 文生图部署场景 | 内存压缩4倍，推理加速1.5倍 | 支持动态FP8和Weight-Only FP8量化，适配TensorRT Diffusion插件 | ✅ **x86环境** |

### 2.3 工程化优化工具适配
| 工具特性 | 功能描述 | 适配方案 | **执行设备** |
|---------|----------|----------|--------------|
| **单卡大模型量化** | 单GPU可量化235B大模型 | 大模型分块量化，自动GPU/CPU内存调度 | ✅ **x86环境** |
| **低内存模式** | 支持大模型在小内存机器上量化 | 层级分块处理，降低内存峰值 | ✅ **x86环境** |
| **量化感知训练** | 支持GPTQ/AWQ量化感知训练，进一步提升低比特精度 | 训练后的模型直接导出为量化格式，适配TensorRT | ✅ **x86环境** |
| **词表缩减** | 领域定制场景，根据领域数据缩减词表 | 词表缩减后模型直接导出，减少推理计算量 | ✅ **x86环境** |
| **MoE模型优化** | 支持Hunyuan-MoE、Qwen3-VL-MoE等混合专家模型量化 | 针对MoE门控和专家层优化量化策略 | ✅ **x86环境** |

---

## 三、端到端优化流水线
```
┌─────────────────────┐    ┌─────────────────────┐    ┌─────────────────────┐    ┌─────────────────────┐
│ HuggingFace 模型    │───▶│ AngelSlim 量化压缩   │───▶│ TensorRT-Edge-LLM   │───▶│ 端侧设备部署        │
│ (PyTorch)           │    │ (量化/词表缩减/      │    │ 导出编译流水线      │    │ (Jetson/DRIVE)      │
│                     │    │ Eagle3训练)         │    │                     │    │                     │
└─────────────────────┘    └─────────────────────┘    └─────────────────────┘    └─────────────────────┘
```

### 3.1 流水线步骤（明确执行设备+具体操作）
| 步骤 | 执行设备 | 具体操作 | 版本要求 | 注意事项 |
|------|----------|----------|----------|----------|
| 1. **模型准备** | ✅ **x86环境** | 从HuggingFace/本地加载原始PyTorch模型（LLM/VLM/Diffusion） | 无特殊要求，建议torch≥2.0 | 不需要同步到端侧，仅x86侧使用 |
| 2. **预处理** | ✅ **x86环境** | <ul><li>领域定制：词表缩减（可选）</li><li>数据准备：校准数据集准备（256-1024条领域数据）</li></ul> | 无特殊要求 | 词表缩减后模型不需要更改结构，可直接用于后续量化 |
| 3. **量化压缩（AngelSlim）** | ✅ **x86环境** | <ul><li>选择对应硬件的量化精度：<br> - Orin系列：FP8/INT4<br> - DRIVE Thor：NVFP4</li><li>修改AngelSlim配置文件，执行量化：`python tools/run.py --config configs/qwen3/fp8_static/qwen3-2b.yaml`</li><li>可选：训练Eagle3草稿模型：`python tools/train_eagle3_offline.py --model Qwen/Qwen3-2B`</li><li>生成量化后的PyTorch权重文件</li></ul> | torch≥2.2，AngelSlim最新版本 | 完全不需要端侧参与，所有量化工作在x86完成，输出标准PyTorch `.pth` 或 HuggingFace格式权重 |
| 4. **ONNX导出** | ✅ **优先x86环境**（端侧可运行但不推荐） | <ul><li>用TensorRT-Edge-LLM导出工具导出ONNX：<br>`tensorrt-edgellm-export-llm --model_dir ./quantized_model --output_dir ./onnx_output`<br>`tensorrt-edgellm-export-visual --model_dir Qwen/Qwen3-VL-2B --output_dir ./onnx_output/visual`</li><li>可选：导出Eagle3草稿模型ONNX</li></ul> | x86侧torch≥2.0，导出时指定opset=17-19 | 导出的ONNX和架构无关，可直接传到端侧编译，完美避开端侧torch版本限制 |
| 5. **TensorRT引擎编译** | ✅ **必须端侧设备运行** | <ul><li>将x86导出的ONNX文件传输到端侧</li><li>用端侧本地的TensorRT+torch2trt 0.5.0编译：<br>`./build/examples/llm/llm_build --onnxDir=./onnx_output --engineDir=./engine_output --maxBatchSize=1 --vlm --fp8`</li><li>针对目标硬件架构优化：指定`-gencode=arch=compute_87,code=sm_87`（Orin系列）</li></ul> | 端侧严格匹配：<br>torch=2.4.0<br>torch2trt=0.5.0<br>和JetPack版本绑定的TensorRT | 必须在端侧编译才能保证和硬件100%兼容，编译时不需要AngelSlim依赖 |
| 6. **部署运行** | ✅ **必须端侧设备运行** | <ul><li>用TensorRT-Edge-LLM C++运行时执行推理：<br>`./build/examples/llm/llm_inference --engineDir=./engine_output --inputFile=test_case.json`</li><li>开启优化开关：<br> `--enable-cuda-graph`（CUDA Graph优化）<br> `--enable-eagle`（Eagle3推测解码）<br> `--kv-cache-fp8`（FP8 KV Cache）</li><li>性能测试、精度验证、业务对接</li></ul> | 端侧无Python依赖，仅需C++运行时 | 所有性能/精度指标必须在端侧实测才有效 |

### 3.2 自动化工具链
集成AngelSlim和TensorRT-Edge-LLM的命令行工具，提供一站式命令：
```bash
# 一站式量化+导出（x86侧执行）
trt-edgellm-optimize-x86 --model Qwen/Qwen3-VL-2B \
  --quantization fp8_lepto \
  --enable-eagle3 \
  --target-hardware agx_orin \
  --output-dir ./optimized_model
```
执行完成后将`./optimized_model`目录传到端侧，在端侧执行编译：
```bash
# 端侧编译+部署（端侧执行）
trt-edgellm-build-engine --input-dir ./optimized_model --output-dir ./engine
```

### 3.3 具体执行示例（Qwen3-VL-2B + Jetson AGX Orin 32GB）
#### 【x86侧执行步骤】
1. 环境准备：
   ```bash
   # 安装依赖
   pip install angelslim[all]
   pip install tensorrt_edgellm
   ```
2. 量化压缩：
   ```bash
   # 复制配置文件
   cp /path/to/AngelSlim/configs/qwen3/fp8_static/qwen3-2b_fp8_static.yaml ./my_config.yaml
   # 执行FP8量化
   python /path/to/AngelSlim/tools/run.py --config my_config.yaml
   # 可选：训练Eagle3草稿模型
   python /path/to/AngelSlim/tools/train_eagle3_offline.py --model ./output/quantized_qwen3_2b --output ./eagle3_draft
   ```
3. ONNX导出：
   ```bash
   # 导出LLM ONNX
   tensorrt-edgellm-export-llm --model_dir ./output/quantized_qwen3_2b --output_dir ./onnx
   # 导出视觉编码器ONNX
   tensorrt-edgellm-export-visual --model_dir Qwen/Qwen3-VL-2B-Instruct --output_dir ./onnx/visual
   # 导出Eagle3草稿模型ONNX（可选）
   tensorrt-edgellm-export-llm --model_dir ./eagle3_draft --output_dir ./onnx/eagle3
   ```
4. 传输文件到端侧：
   ```bash
   scp -r ./onnx nvidia@172.16.119.188:/home/nvidia/workspace/
   ```

#### 【端侧设备执行步骤】（torch=2.4.0，torch2trt=0.5.0）
1. 编译TensorRT引擎：
   ```bash
   cd /home/nvidia/workspace/TensorRT-Edge-LLM
   ./build/examples/llm/llm_build --onnxDir=../onnx --engineDir=../engine --maxBatchSize=1 --vlm --fp8 --sm 87
   ```
2. 运行推理：
   ```bash
   ./build/examples/llm/llm_inference --engineDir=../engine \
     --inputFile=test_vlm_case.json \
     --enable-cuda-graph \
     --kv-cache-fp8
   ```
3. 开启Eagle3加速（可选）：
   ```bash
   ./build/examples/llm/llm_inference_spec_decode --engineDir=../engine \
     --draft-engine-dir=../engine/eagle3 \
     --inputFile=test_vlm_case.json \
     --enable-cuda-graph
   ```

---

## 三、全模型支持矩阵（AngelSlim + TensorRT-Edge-LLM）
| 模型类型 | 支持的具体模型 | 端侧部署最佳量化方案 |
|---------|----------------|----------------------|
| **LLM语言模型** | Qwen3全系列（0.6B/1.7B/4B/8B/14B/32B/235B）、Qwen2.5全系列、Hunyuan全系列、GLM-4.6、DeepSeek V3/R1、Llama3系列 | 2B以下：FP8<br>2B-7B：INT4-AWQ/GPTQ<br>7B以上：仅适合DRIVE Thor平台用NVFP4 |
| **VLM多模态模型** | Qwen3-VL全系列（2B/7B/8B）、Qwen2.5-VL全系列、HunyuanVL、HunyuanOCR | 2B-3B：FP8<br>7B以上：INT4-AWQ |
| **Audio音频模型** | Qwen2-Audio、Fun-CosyVoice3 | FP8/INT8动态量化 |
| **Diffusion生成模型** | FLUX、SDXL、Qwen-Image、Wan、Hunyuan-Image/Video/3D | FP8动态量化 + DeepCache |
| **MoE混合专家模型** | Hunyuan-MoE、Qwen3-VL-MoE | INT4量化 + 专家路由优化 |

---

## 四、分硬件最佳实践

### 4.1 Jetson AGX Orin 32GB
**推荐配置**：
- 模型大小：2B-7B
- 量化精度：
  - 2B-3B模型：FP8/LeptoQuant
  - 7B模型：INT4-AWQ
- 加速配置：
  - 开启Eagle3推测解码（2.5x加速）
  - 开启CUDA Graph（15%加速）
  - 开启FP8 KV Cache
- 预期性能：
  - Qwen3-VL-2B：25-35 token/s
  - Qwen3-VL-7B：15-20 token/s
  - 视觉编码：< 40ms

### 4.2 Jetson Orin NX 16GB
**推荐配置**：
- 模型大小：1B-4B
- 量化精度：INT4-AWQ/GPTQ
- 加速配置：
  - 开启Eagle3
  - 限制batch size ≤2
  - KV Cache大小 ≤2048
- 预期性能：
  - Qwen3-1.7B：30-40 token/s
  - Qwen3-4B：15-20 token/s

### 4.3 Jetson Orin NX 8GB
**推荐配置**：
- 模型大小：0.5B-1.5B
- 量化精度：INT4-AWQ 或 W4A8
- 加速配置：
  - 开启Eagle3
  - 序列长度 ≤1024
- 预期性能：
  - Qwen3-0.6B：40-50 token/s

### 4.4 DRIVE Thor（Blackwell）
**推荐配置**：
- 模型大小：7B-32B
- 量化精度：NVFP4
- 加速配置：
  - 开启Eagle3
  - 开启NVFP4 KV Cache
- 预期性能：
  - Qwen3-8B：80-100 token/s
  - Qwen3-32B：30-40 token/s

---

## 五、全模型支持矩阵（AngelSlim + TensorRT-Edge-LLM）
| 模型类型 | 支持的具体模型 | 端侧部署最佳量化方案 |
|---------|----------------|----------------------|
| **LLM语言模型** | Qwen3全系列（0.6B/1.7B/4B/8B/14B/32B/235B）、Qwen2.5全系列、Hunyuan全系列、GLM-4.6、DeepSeek V3/R1、Llama3系列 | 2B以下：FP8<br>2B-7B：INT4-AWQ/GPTQ<br>7B以上：仅适合DRIVE Thor平台用NVFP4 |
| **VLM多模态模型** | Qwen3-VL全系列（2B/7B/8B）、Qwen2.5-VL全系列、HunyuanVL、HunyuanOCR | 2B-3B：FP8<br>7B以上：INT4-AWQ |
| **Audio音频模型** | Qwen2-Audio、Fun-CosyVoice3 | FP8/INT8动态量化 |
| **Diffusion生成模型** | FLUX、SDXL、Qwen-Image、Wan、Hunyuan-Image/Video/3D | FP8动态量化 + DeepCache |
| **MoE混合专家模型** | Hunyuan-MoE、Qwen3-VL-MoE | INT4量化 + 专家路由优化 |

---

## 六、AngelSlim预训练量化模型库（开箱即用，无需自行量化）
| 模型名称 | 量化精度 | 适用硬件 | 下载地址 |
|---------|----------|----------|----------|
| Qwen3-1.7B_eagle3 | FP8 | Jetson Orin NX/AGX Orin | HuggingFace: AngelSlim/Qwen3-1.7B_eagle3 |
| Qwen3-8B_eagle3 | INT4-AWQ | Jetson AGX Orin 32GB | HuggingFace: AngelSlim/Qwen3-8B_eagle3 |
| Qwen3-VL-2B-Instruct_eagle3 | FP8 | Jetson AGX Orin 32GB | HuggingFace: AngelSlim/Qwen3-VL-2B-Instruct_eagle3 |
| Qwen3-32B-NVFP4 | NVFP4 | DRIVE Thor | HuggingFace: AngelSlim/Qwen3-32B-NVFP4 |
| Hunyuan-A13B-Instruct_fp8_static | FP8 | DRIVE Thor | HuggingFace: AngelSlim/Hunyuan-A13B-Instruct_fp8_static |
| Hunyuan-7B-Instruct_int4_gptq | INT4-GPTQ | Jetson AGX Orin 32GB | HuggingFace: AngelSlim/Hunyuan-7B-Instruct_int4_gptq |
| HunyuanOCR_eagle3 | FP8 | Jetson AGX Orin | HuggingFace: AngelSlim/HunyuanOCR_eagle3 |
| DeepSeek-R1-0528_w4a8_fp8 | W4A8混合精度 | Jetson AGX Orin 32GB | HuggingFace: AngelSlim/DeepSeek-R1-0528_w4a8_fp8 |
| Fun-CosyVoice3-0.5B-2512_eagle3 | FP8 | Jetson Orin NX | HuggingFace: AngelSlim/Fun-CosyVoice3-0.5B-2512_eagle3 |
| HY-1.8B-2Bit | 2bit | Jetson Nano/Orin NX 8GB | HuggingFace: AngelSlim/HY-1.8B-2Bit |

---

## 七、性能评测工具链
| 评测类型 | 工具名称 | 功能描述 | **执行设备** |
|---------|----------|----------|--------------|
| **精度评测** | LM-Evaluation-Harness | 评测CEVAL、MMLU、GSM8K、HumanEval等LLM任务精度 | ✅ **x86环境**（量化后先在x86验证精度，避免端侧重复工作） |
| **多模态精度评测** | MMMU/MMStar | 评测VLM模型的多模态理解能力 | ✅ **x86环境** |
| **推理性能评测** | vLLM Benchmark | 测试吞吐量、延迟、Eagle3接收长度等指标 | ✅ **x86环境** |
| **端侧性能评测** | TensorRT-Edge-LLM Benchmark | 测试端侧实际运行的延迟、吞吐量、显存占用、功耗等 | ✅ **端侧设备**（必须在真实硬件测试） |
| **压缩效果评测** | Perplexity测试 | 量化后模型困惑度对比 | ✅ **x86环境** |

---

## 八、部署工具链集成
| 部署场景 | 工具方案 | 操作命令 | **执行设备** |
|---------|----------|----------|--------------|
| **离线推理验证** | AngelSlim离线推理工具 | `python scripts/deploy/offline.py $MODEL_PATH "输入问题"` | ✅ **x86环境** |
| **服务化部署（云端）** | vLLM/SGLang服务 | `bash scripts/deploy/run_vllm.sh --model-path $MODEL_PATH --port 8080` | ✅ **x86环境** |
| **端侧部署** | TensorRT-Edge-LLM C++运行时 | 见3.3节端侧执行命令 | ✅ **端侧设备** |
| **精度自动化验证** | LM-Eval自动化脚本 | `bash scripts/deploy/lm_eval.sh --tasks ceval-valid,mmlu $MODEL_PATH` | ✅ **x86环境** |

---

## 九、精度保证体系
### 9.1 量化精度验证（✅ **x86环境执行**）
- 使用AngelSlim自带的评测工具链（lm_eval/lmms_eval）验证量化后模型精度
- 精度损失超过2%时自动降级到更高精度或调整量化参数
- 针对关键任务（如车载OCR、工业质检）定制验证数据集
- 验证通过后再进行后续导出编译步骤，避免端侧重复工作

### 9.2 端到端验证（✅ **端侧设备执行**）
- 量化后模型在TensorRT-Edge-LLM运行时的精度对齐验证（和x86侧量化结果对比）
- 性能基准测试：延迟、吞吐量、显存占用（必须在真实端侧设备测试）
- 压力测试：长时间运行稳定性验证
- 功耗测试：验证在端侧设备的功耗表现

## 十、端侧torch 2.4.0版本适配方案
### 10.1 完全避开版本限制的最佳实践
1. **端侧不需要安装AngelSlim**：所有量化、训练、导出工作全部在x86完成，端侧仅需TensorRT-Edge-LLM的C++运行时
2. **端侧不需要升级任何Python包**：保持现有torch 2.4.0、torch2trt 0.5.0版本不变，仅用于编译TensorRT引擎
3. **x86侧导出ONNX时保证兼容性**：指定opset版本为17-19，不使用torch 2.4.0不支持的新算子

### 10.2 特殊场景适配（必须在端侧做量化）
如果因数据安全等原因必须在端侧执行量化：
- 降级AngelSlim到v0.2.0版本（该版本兼容torch 2.4.0）
- 仅执行PTQ量化，不要执行Eagle3训练等计算密集型任务
- 限制校准数据集大小≤128条，避免端侧内存不足

---

## 十一、实施路线图

### 阶段一：基础功能集成（1-2周）
1. ✅ 集成AngelSlim FP8/INT4量化算法到TensorRT-Edge-LLM导出流水线
2. ✅ 支持FP8/INT4量化模型的TensorRT引擎编译
3. ✅ 验证Qwen3/Llama系列模型量化精度和性能

### 阶段二：高级特性集成（2-3周）
1. ⬜ 集成LeptoQuant、SmoothQuant等高级量化算法
2. ⬜ 集成Eagle3推测解码全流程（训练+导出+部署）
3. ⬜ 支持多模态模型（Qwen3-VL）的端到端优化

### 阶段三：极致优化（3-4周）
1. ⬜ 集成NVFP4量化支持（Blackwell架构）
2. ⬜ 集成SpecExit思考早退优化
3. ⬜ 支持极低比特量化（2bit/1.25bit）
4. ⬜ 完成自动化工具链开发

### 阶段四：场景落地（持续）
1. ⬜ 针对车载、机器人、工业等场景定制优化方案
2. ⬜ 建立场景化性能基准库
3. ⬜ 持续优化工具链易用性和稳定性

---

## 十二、预期收益对比
以 **Jetson AGX Orin 32GB + Qwen3-VL-2B** 为例：

| 优化阶段 | 精度 | 显存占用 | 生成速度 | 加速比 |
|---------|------|----------|----------|--------|
| 原生FP16 | 100% | ~16GB | 8-10 token/s | 1x |
| FP8量化 | 99.5% | ~8GB | 12-15 token/s | 1.5x |
| FP8 + Eagle3 | 99.5% | ~9GB | 25-35 token/s | 3x-3.5x |
| INT4 + Eagle3 | 98% | ~5GB | 30-40 token/s | 3.5x-4x |

---

## 十三、关键参考资源
1. AngelSlim 官方文档：https://angelslim.readthedocs.io/
2. TensorRT-Edge-LLM 开发者文档：`./docs/source/developer_guide/`
3. Eagle3 技术报告：https://arxiv.org/abs/2509.24248
4. LeptoQuant 技术报告：https://arxiv.org/abs/2601.07892
