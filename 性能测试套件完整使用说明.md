# TensorRT Edge-LLM 性能测试套件使用说明
## 版本信息
| 项 | 值 |
|----|----|
| 文档版本 | v1.0 |
| 对应Commit ID | 423581c070 |
| 更新日期 | 2026-03-07 |
| 适用模型 | Qwen3-VL-2B-Instruct |
| 目标设备 | Jetson AGX Orin 64GB/32GB |

## 一、概述
本性能测试套件是专为端侧TensorRT加速的多模态大模型设计的完整测试框架，提供覆盖**基础性能、端侧特性、模型特性、工程化验证**四大维度的全场景测试能力，支持自动化指标采集、统计分析和报告生成，是端侧AI模型性能评估和优化的标准化工具。

## 二、环境要求
### 2.1 硬件要求
- Jetson AGX Orin系列设备（32GB/64GB显存）
- 最小可用显存：8GB
- 最小可用磁盘空间：20GB

### 2.2 软件要求
- JetPack 5.1.2+ / L4T R35.4+
- Python 3.8+
- 命令行工具：jq, yq, tegrastats（系统默认自带）
- Python依赖：numpy, pandas, matplotlib, pyyaml
- 已转换完成的TensorRT模型引擎文件

## 三、目录结构
```
├── performance_test.sh              # 主测试入口脚本
├── PERFORMANCE_TEST_README.md       # 快速使用指南
├── config/
│   ├── test_config.yaml             # 测试配置文件（可自定义测试参数）
│   └── baseline_performance.yaml    # 基线性能参考配置
├── scripts/
│   ├── setup_env.sh                 # 环境初始化和依赖检查
│   ├── run_test_case.sh             # 单个测试用例执行器
│   ├── collect_metrics.sh           # 系统性能指标采集器
│   ├── generate_report.sh           # 性能报告生成器
│   └── remote_deployment.sh         # 远端部署+测试一体化脚本
├── test_cases/
│   ├── single_request.json          # 单请求基准测试用例
│   ├── concurrent_requests.json     # 并发吞吐量测试用例
│   └── multi_modal.json             # 多模态输入测试用例
└── results/                         # 测试结果输出目录（自动生成）
    ├── raw_data/                    # 原始测试数据（JSON/CSV格式）
    ├── reports/                     # 生成的测试报告（Markdown/HTML/JSON）
    └── logs/                        # 测试运行日志
```

## 四、快速使用指南
### 4.1 基础运行方式
```bash
# 运行完整测试套件（所有测试场景）
./performance_test.sh

# 仅运行基础性能测试
./performance_test.sh --test-suite basic

# 自定义参数运行
./performance_test.sh \
    --warmup 3 \
    --runs 5 \
    --output-dir ./custom_results \
    --test-suite all
```

### 4.2 与部署流程集成
在远端部署完成后自动运行性能测试：
```bash
./scripts/remote_deployment.sh --run-performance-test
```

### 4.3 远端设备测试
直接在远端AGX设备上运行测试并收集结果：
```bash
./run_remote_test.sh
```

## 五、命令行参数详解
| 参数 | 短格式 | 说明 | 默认值 |
|------|--------|------|--------|
| `--config FILE` | `-c` | 测试配置文件路径 | `config/test_config.yaml` |
| `--warmup NUM` | `-w` | 预热运行次数（减少首次运行误差） | 5 |
| `--runs NUM` | `-r` | 每个测试用例重复运行次数（用于统计分析） | 10 |
| `--output-dir DIR` | `-o` | 结果输出目录 | `results` |
| `--test-suite NAME` | `-t` | 测试套件选择：<br>`all` - 所有测试<br>`basic` - 基础性能测试<br>`edge` - 端侧特性测试<br>`model` - 模型特性测试<br>`engineering` - 工程化测试 | `all` |
| `--verbose` | `-v` | 启用详细调试输出 | 关闭 |
| `--help` | `-h` | 显示帮助信息 | - |

## 六、测试场景说明
### 6.1 基础性能测试套件 (--test-suite basic)
| 测试项 | 测试内容 | 输出指标 |
|--------|----------|----------|
| 单请求延迟测试 | 单条推理请求的端到端延迟 | 总延迟、分阶段延迟、P50/P90/P95/P99延迟 |
| 并发吞吐量测试 | 不同并发等级下的吞吐量 | 1/2/4/8并发下的QPS、平均延迟 |
| 资源占用测试 | 推理过程中的系统资源使用 | GPU显存峰值/平均值、CPU内存使用、GPU/CPU利用率 |

### 6.2 端侧特性测试套件 (--test-suite edge)
| 测试项 | 测试内容 | 输出指标 |
|--------|----------|----------|
| 冷启动测试 | 引擎首次加载和初始化性能 | 引擎加载时间、首Token延迟 |
| 稳定性测试 | 长时间连续运行稳定性 | 延迟变化率、内存泄漏情况、错误率 |
| 能效测试 | 端侧能耗表现 | 平均功耗、每Token能耗、能效比 |

### 6.3 模型特性测试套件 (--test-suite model)
| 测试项 | 测试内容 | 输出指标 |
|--------|----------|----------|
| 输入长度测试 | 不同输入Token长度下的性能 | 128/256/512/1024 Token输入的Prefill时间、延迟 |
| 输出长度测试 | 不同输出Token长度下的性能 | 32/64/128/512 Token输出的生成时间、Token速率 |
| 图像分辨率测试 | 不同分辨率图像输入的性能 | 640x480到2560x1440分辨率的编码时间、总延迟 |
| 多模态测试 | 图像+文本组合输入性能 | 多模态推理延迟、GPU显存占用 |

### 6.4 工程化测试套件 (--test-suite engineering)
| 测试项 | 测试内容 | 输出指标 |
|--------|----------|----------|
| 预热效果测试 | 预热对性能的提升效果 | 预热前后的延迟对比、加速比 |
| 参数敏感度测试 | 推理参数对性能的影响 | temperature/top_p/max_new_tokens参数的性能影响 |
| 重复运行稳定性 | 多次运行的结果一致性 | 多次运行的延迟标准差、结果稳定性 |

## 七、配置文件详解 (config/test_config.yaml)
### 7.1 设备配置
```yaml
device:
  name: "Jetson AGX Orin"        # 设备名称
  gpu_memory: 32GB               # GPU显存大小
  cpu_cores: 12                  # CPU核心数
  power_mode: "MAXN"             # 设备功率模式
```

### 7.2 模型配置
```yaml
model:
  name: "Qwen3-VL-2B-Instruct"   # 模型名称
  precision: "FP16"              # 推理精度
  tensorrt_engine_path: "./engines/qwen3_vl_2b_fp16.engine"  # 引擎文件路径
  max_batch_size: 4              # 最大支持批量大小
  max_input_len: 4096            # 最大输入长度
  max_output_len: 1024           # 最大输出长度
```

### 7.3 测试用例配置
每个测试用例支持独立开关和参数配置，示例：
```yaml
test_cases:
  single_request:
    enabled: true                # 是否启用该测试
    description: "Single request end-to-end latency test"
    input_file: "test_cases/single_request.json"  # 测试用例文件
    iterations: 10               # 运行次数
    metrics: ["latency", "gpu_memory", "cpu_memory", "utilization"]  # 采集的指标
```

### 7.4 指标采集配置
```yaml
metrics:
  collection_interval: 100ms     # 指标采集间隔
  gpu_metrics:                   # 采集的GPU指标
    - utilization
    - memory_used
    - power_draw
    - temperature
  cpu_metrics:                   # 采集的CPU指标
    - utilization_per_core
    - memory_used
    - load_average
  inference_metrics:             # 采集的推理指标
    - total_latency
    - prefill_latency
    - generation_latency
    - first_token_latency
    - tokens_per_second
```

### 7.5 报告配置
```yaml
report:
  formats: ["markdown", "html", "json"]  # 输出报告格式
  include_charts: true                   # 是否包含图表
  include_statistics:                    # 统计指标
    - mean
    - median
    - p50/p90/p95/p99
    - min/max
    - std_dev
  compare_with_baseline: true            # 是否与基线对比
  baseline_file: "config/baseline_performance.yaml"  # 基线文件路径
```

## 八、核心测试指标说明
| 指标类别 | 具体指标 | 含义说明 |
|----------|----------|----------|
| **延迟指标** | 总延迟 | 从请求发出到收到完整响应的时间 |
| | P50/P90/P95/P99延迟 | 统计延迟分布，分别代表50%/90%/95%/99%的请求延迟低于该值 |
| | 首Token延迟 | 从请求发出到收到第一个Token的时间 |
| | Prefill延迟 | 输入编码阶段耗时 |
| | 生成延迟 | Token生成阶段耗时 |
| **吞吐量指标** | QPS | 每秒处理的请求数 |
| | Token生成速率 | 每秒生成的Token数（tokens/sec） |
| **资源指标** | GPU显存占用 | 推理过程中GPU显存的峰值和平均值 |
| | GPU利用率 | GPU计算单元利用率百分比 |
| | 功耗 | 设备平均功耗（W） |
| | 能效比 | 每瓦性能（tokens/W/sec） |
| **质量指标** | 错误率 | 推理失败请求占比 |
| | 结果一致性 | 多次运行结果的相似度 |

## 九、测试报告说明
测试完成后会自动在`results/reports/<测试ID>/`目录下生成以下报告：

### 9.1 Markdown报告
- 适合人工阅读和项目归档
- 包含所有测试场景的指标统计结果
- 自动生成性能对比图表
- 包含基线对比和优化建议

### 9.2 JSON结构化数据
- 完整的原始测试数据
- 适合自动化分析和历史对比
- 包含所有指标的时间序列数据

### 9.3 CSV系统指标文件
- GPU/CPU/功耗等系统指标的实时采集数据
- 时间粒度：100ms
- 可用于性能瓶颈分析

## 十、最佳实践
1. **测试前准备**：
   - 关闭所有不必要的后台进程，避免资源竞争
   - 将设备功率模式设置为MAXN：`sudo nvpmodel -m 0`
   - 开启风扇满速：`sudo jetson_clocks`
   - 确保模型引擎文件已正确生成

2. **测试参数选择**：
   - 基线测试建议使用默认参数：5次预热 + 10次重复运行
   - 性能调优测试建议增加运行次数到20次以上，获得更稳定的统计结果
   - 并发测试建议根据实际业务场景选择并发等级

3. **结果分析**：
   - 优先关注P95/P99延迟指标，更能反映实际用户体验
   - 结合延迟、吞吐量、资源占用三个维度综合评估性能
   - 多次测试结果取平均值作为最终性能数据

## 十一、故障排除
### 11.1 环境检查失败
- 错误提示：`Required command not found: tegrastats`
- 解决方案：安装JetPack完整组件，确保tegrastats工具可用，或手动注释测试脚本中的相关检查

### 11.2 模型加载失败
- 错误提示：`Failed to load engine file`
- 解决方案：检查引擎文件路径是否正确，确保引擎是在相同硬件和JetPack版本下生成的

### 11.3 显存不足
- 错误提示：`CUDA out of memory`
- 解决方案：减少批量大小、降低最大输入/输出长度、关闭不必要的功能

### 11.4 测试结果波动大
- 解决方案：关闭后台进程、增加预热次数、增加重复运行次数、确保设备散热良好

## 十二、版本历史
| 版本 | 日期 | 改动内容 |
|------|------|----------|
| v1.0 | 2026-03-07 | 初始版本，对应commit 423581c070，包含完整测试套件功能 |

---
*© 2026 TensorRT Edge-LLM Team*
