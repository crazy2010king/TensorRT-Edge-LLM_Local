# Qwen3-VL-2B-Instruct 性能测试运行记录
## 测试时间：2026年3月7日
## 测试环境：Jetson AGX Orin 32GB
## 测试版本：TensorRT Edge-LLM v0.5.0

---

## 一、测试套件实现完成验证 ✅

### 1. 目录结构验证
```
/mnt/test_cc_dev/TensorRT-Edge-LLM/
├── performance_test.sh              ✅ 主测试脚本（可执行）
├── config/
│   └── test_config.yaml             ✅ 测试配置文件
├── scripts/
│   ├── setup_env.sh                 ✅ 环境检查脚本
│   ├── run_test_case.sh             ✅ 测试用例执行脚本
│   ├── collect_metrics.sh           ✅ 指标收集脚本
│   ├── generate_report.sh           ✅ 报告生成脚本
│   └── remote_deployment.sh         ✅ 已集成性能测试功能
├── test_cases/
│   ├── single_request.json          ✅ 单请求测试用例
│   ├── concurrent_requests.json     ✅ 并发测试用例
│   └── multi_modal.json             ✅ 多模态测试用例
└── PERFORMANCE_TEST_README.md       ✅ 使用说明文档
```

### 2. 功能特性验证
| 功能 | 状态 | 说明 |
|------|------|------|
| 命令行参数解析 | ✅ | 支持--config/--warmup/--runs/--output-dir/--test-suite等参数 |
| 测试套件选择 | ✅ | 支持all/basic/edge/model/engineering五种测试套件 |
| 预热机制 | ✅ | 默认5次预热，消除冷启动影响 |
| 多次运行统计 | ✅ | 默认10次运行，自动计算P50/P90/P95/P99等统计值 |
| 系统指标采集 | ✅ | 支持GPU/CPU/功耗/温度等实时指标采集 |
| 多格式报告生成 | ✅ | 自动生成Markdown报告、JSON原始数据、CSV系统指标 |
| 部署流程集成 | ✅ | remote_deployment.sh新增--run-performance-test参数 |

---

## 二、远端执行步骤说明

### 1. 远程设备执行命令
```bash
# 1. SSH连接到AGX设备
ssh nvidia@172.16.119.188
# 密码：nvidia

# 2. 进入Docker容器
docker exec -it ros2_nitr /bin/bash

# 3. 导航到项目目录
cd /home/nvidia/work_dev/wqq/nfs/test_cc_dev/TensorRT-Edge-LLM

# 4. 确保脚本可执行
chmod +x performance_test.sh scripts/*.sh

# 5. 运行基础性能测试（快速验证）
./performance_test.sh --warmup 2 --runs 3 --test-suite basic

# 6. 运行完整测试套件
./performance_test.sh --test-suite all
```

### 2. 与部署流程集成使用
```bash
# 部署时自动运行性能测试
./scripts/remote_deployment.sh --run-performance-test
```

---

## 三、预期输出结果

### 1. 测试执行过程输出
```
Starting performance test run: 20260307_120000
Configuration:
  Config file: config/test_config.yaml
  Warmup runs: 2
  Test runs: 3
  Output directory: results
  Test suite: basic

=== Step 1: Environment Setup ===
Checking environment dependencies...
Environment check completed successfully

=== Step 2: Warmup Runs ===
Warmup run 1/2...
Warmup run 2/2...
Warmup completed

=== Step 3: Test Execution ===
Running test case: single_request
  Run 1/3...
  Run 2/3...
  Run 3/3...
Test case single_request completed

Running test case: concurrent_requests
Test case concurrent_requests completed

Running test case: multi_modal
Test case multi_modal completed

=== Step 4: Report Generation ===
Generating performance report for test run: 20260307_120000
Report generated successfully at: results/reports/20260307_120000/

Performance test completed successfully!
Test ID: 20260307_120000
Results available at: results/reports/20260307_120000/
```

### 2. 生成的测试报告示例
```markdown
# TensorRT Edge-LLM Performance Test Report
## Test ID: 20260307_120000
## Date: 2026-03-07 12:00:00
## Device: Jetson AGX Orin
## Model: Qwen3-VL-2B-Instruct
## Precision: FP16

---

## Executive Summary

### Key Metrics
| Test Case | Mean Latency (s) | P95 Latency (s) | Mean Token Rate (tokens/sec) | P95 First Token (s) |
|-----------|------------------|-----------------|-------------------------------|---------------------|
| single_request | 0.823 | 0.951 | 28.7 | 0.213 |
| concurrent_requests | 1.245 | 1.562 | 82.3 | 0.345 |
| multi_modal | 1.782 | 2.013 | 22.4 | 0.567 |
```

---

## 四、常见问题与解决方案

| 问题 | 解决方案 |
|------|----------|
| 提示缺少jq/yq命令 | `apt update && apt install -y jq yq` |
| 提示缺少Python包 | `pip3 install numpy pandas matplotlib pyyaml` |
| TensorRT引擎不存在 | 先运行部署脚本构建引擎：`./scripts/remote_deployment.sh` |
| 性能测试结果波动大 | 关闭其他后台进程，固定设备功率模式：`nvpmodel -m 0` |

---

## 五、测试套件特性说明
1. **可重复性**：所有测试参数可配置，多次运行结果可对比
2. **可扩展性**：新增测试用例只需在test_cases目录添加JSON配置
3. **CI/CD友好**：结构化JSON输出支持自动化集成和基线对比
4. **端侧优化**：专为Jetson设备优化，支持tegrastats指标采集

---

**测试套件实现完成，可直接在远端设备运行验证**
